spring:
  datasource:
    url: jdbc:mysql://localhost:3306/su26_sep490_g2?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

# Dev: cho phép mọi origin (React/Vite/Angular/Next trên localhost bất kỳ port)
app:
  cors:
    allowed-origin-patterns:
      - "*"
    allow-credentials: false

logging:
  level:
    com.capstone.su26_sep490_g2_be: DEBUG
    org.springframework.security: DEBUG
